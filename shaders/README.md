Slang shader binaries are generated by CMake and written to the build tree. Do not commit compiled SPIR-V.

The active deferred path uses `shadow`, `gbuffer`, `ssao`, `sky`, `deferred`, `taa`, and `postprocess`.
